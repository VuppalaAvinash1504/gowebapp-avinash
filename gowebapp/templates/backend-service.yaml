apiVersion: v1
kind: Service
metadata:
  name: gowebapp-backend
spec:
  selector:
    app: gowebapp-backend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}